/**
 * Largura do conteúdo nas páginas do painel.
 * O `<main>` já tem padding; usar `w-full` evita faixas estreitas (`max-w-xl`) no desktop.
 */
export const painelPageContentWidthClass = "w-full max-w-none";
